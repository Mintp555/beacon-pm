# Beacon PM

Static marketing site for Beacon PM — project management for healthcare
quality improvement and CARF accreditation.

The site lives in [`site/`](site/) (plain HTML/CSS). It is published to
GitHub Pages by the workflow in `.github/workflows/deploy.yml` on every
push to `main`.
